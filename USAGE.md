# 🍳 Diet Manager 使用手册

一份给"用 app 的你自己"看的指南。功能上的开发细节看 `CLAUDE.md`，环境搭建看 `README.md`。这份手册聚焦：**怎么用得顺手**。

---

## 📖 快速上手（5 分钟）

### 1️⃣ 启动 app

```bash
python3.9 -m streamlit run app.py
```

浏览器自动打开 `http://localhost:8501`。侧边栏 6 个页面：

- 📦 **库存** — 你冰箱/储物柜里有什么
- 🍳 **菜谱库** — 你会做什么
- 🌌 **食愿之书** — 命中注定要做但还没排上的菜
- 📋 **购物清单** — 按超市分别记今天/这周要买啥
- 📅 **今日规划** — 今晚做什么
- 📊 **营养分析** — 吃下去算多少营养

> 📸 *[截图：侧边栏导航]*

### 2️⃣ 录入你的第一批食材

打开 📦 **库存**，最快的方式是顶部的「⚡ AI 批量录入」expander：

```
粘贴：黄瓜2份 羊肉3份 秋葵1份 半份生菜 一包冻虾仁
点击：🤖 AI 解析
```

预览里调整一下分类（蛋白/叶菜/调味/干货/其他都可选），点「✅ 确认入库」搞定。

> 💡 **小贴士**：第一次用建议至少录 10-15 种食材（叶菜 5+、蛋白 3-5、调味干货按现状勾），不然推荐器没东西可选。

### 3️⃣ 推荐你的第一顿晚餐

打开 📅 **今日规划**，点「🎲 推荐组合」。等几秒，会得到 1-2 个候选套餐，每个 4 道菜（目标：1 凉拌 + 2 热菜 + 1 汤），下面会写：

- ⏱️ 总时长 / 难度
- 🍲 是否含汤
- 🍱 菜系（如果联动成功）
- 🔴 覆盖的易坏食材

挑一个顺眼的，点「✅ 采用此套餐」，菜谱自动进入「📋 今日菜单」。

> 💡 不喜欢推荐结果？再点一次「🎲 推荐组合」，背后是加权随机，每次都不同。

### 4️⃣ 打印菜单上灶

「今日菜单」右上角「🖨️ 打印今日菜单」→ PDF 自动生成并打开：

- **正面**：今晚菜单（中英文 + 诗意简介）+ 全日营养摘要
- **背面**：4 道菜的执行卡（食材分主料/调料 + 步骤）+ 底部 **Mise en place** 备料总览（所有主料 + 所有调料）

打印两面 → 翻过来看是后厨执行手册，正面是仪式感菜单。

> 💡 想调整今晚水果/主食/临时加菜？展开「🎛️ 备餐控制台」改完直接打印，无需切到营养页计算。

---

## 🔁 典型工作流

### 每周一次：采购 + 补库存

**采购前（出门前 5 分钟）**：
1. 打开 📦 库存，看顶部 metric
   - **🔴 易坏优先**：本周菜单优先围绕这些做（如豆苗、韭菜要吃完）
   - **天份**估算：叶菜 < 4 天份 / 蛋白 < 3 天份 → 该补了
2. 顺便扫一眼调味/干货 tab，缺货的（⬜ 灰色按钮）记到购物清单

**采购回来（10 分钟搞定）**：
- 想偷懒：「⚡ AI 批量录入」一次性粘贴整个购物清单
  ```
  鸡腿肉2包 鲈鱼1条 黄瓜3根 西红柿4个 生菜1袋 蓝莓2盒
  ```
  AI 自动判断分类 + 是否易坏，预览校对后批量入库
- 想精确：「🛒 购物模式」toggle 打开 → 逐项填份数 → 点录入

**调味/干货**：用完才需要点亮/关闭。日常补满不用记数量。

> 💡 加新品类的食材（之前没买过的）：在 expander 「➕ 添加条目」里设好 `portion_weight_g`（默认 500g 叶菜 / 300g 蛋白）。AI 批量录入会自动按分类给默认值。

### 每天 17:00：规划晚餐

我的实际流程（5 分钟）：

1. **看库存（30 秒）**：📦 库存 → 头部「🔴 易坏优先」如果非零，今天优先消耗它们
2. **生成推荐（1 分钟）**：📅 今日规划 → 「🎲 推荐组合」
   - 看推荐卡片：覆盖了易坏？菜系联动到什么？时长能不能接受？
   - 不满意：再点一次，加权随机每次不同
3. **微调（2 分钟）**：
   - 套用了一个套餐 → 「📋 今日菜单」出现
   - 如果想替换 1-2 道：✕ 删掉 → 下面选菜区找替换菜
   - 看到「💡 推荐搭配」banner？一键加入灵魂伴侣
4. **备餐 + 打印（1 分钟）**：
   - 展开「🎛️ 备餐控制台」：今晚不吃米饭就改主食，临时想加莴笋就填进加菜框
   - 「🖨️ 打印今日菜单」→ PDF 自动开
5. **上灶**：背面执行卡 + 底部 Mise en place 按图索骥

> 💡 **三选一不矛盾**：纯推荐器、纯手动、混合都行。推荐 1 道 + 手动选 3 道完全可以，「💡 推荐搭配」就是为这种场景设计的。

### 每天 21:00（可选）：记录营养

> 💡 **新行为**（2026-05+）：你在「📅 今日规划」点「✅ 确认菜单并扣减库存」时，系统会自动用当前备餐控制台的设置算一次全日营养并保存到历史。所以下面这步是**可选的精修**，纯净忘了点确认或想覆盖默认值时才需要。

**自动记录场景（推荐）**：
- 上完晚饭 → 📅 今日规划 → 「✅ 确认菜单并扣减库存」→ 自动完成扣减 + 标记已做 + 保存营养记录三件事

**手动精修场景**：
1. 📊 **营养分析** → 默认进入「🌅 全日营养」tab
2. 备餐控制台改过的水果/主食/加菜**自动同步过来**（同 `key` 跨页绑定）
3. 早午餐情况：
   - 正常吃 → 默认值已经填好，不用动
   - 没在家吃 → 「今日不在家吃X饭」toggle
   - 吃了非默认 → 展开「✏️ 特殊情况：自定义食材」，按行填 `食材名 数量 单位`
4. 点「📊 计算今日全日营养」→ 看 14 种营养素 vs DRI 进度条
5. 点蓝色「💾 保存今日记录」入库（**同日重复保存会 upsert 覆盖**，不会重复）

**每周复盘**：「📈 历史记录」tab → 7 日 DRI 热力图，看哪些营养素长期 🔴/🟡，下周采购针对性补（铁不足 → 多买红肉菠菜；维 D 不足 → 三文鱼蛋黄）。

> 💡 想要 AI 给具体建议？「🤖 AI 营养建议」expander（Gemini，3 次/天限额，按日自动重置）。

---

## 📦 库存功能详解

### 五大分类

| 分类 | 类型 | 典型物品 | 备注 |
|---|---|---|---|
| 🥬 叶菜/时令 | **份**（可数） | 黄瓜、生菜、西红柿 | 默认每份 500g |
| 🥩 蛋白/冷库 | **份**（可数） | 鸡腿、虾仁、豆腐、冷冻饺子 | 默认每份 300g |
| 🧂 调味 | **有/无** | 生抽、老抽、料酒、各类香料 | 不计量 |
| 🏺 干货 | **有/无** | 大米、面粉、坚果、紫菜 | 不计量 |
| 📦 其他 | **有/无** | 洋葱、土豆、姜蒜（常备根茎） | 不计量 |

> 💡 **为什么按"份"而不是按"克"？** 实际生活中"一袋鸡腿""半斤葱"比"237g 葱"自然得多。每个食材可以自定义 `portion_weight_g`，UI 显示 `X 份 ≈ Yg`，两边兼顾。

### 添加食材的三种方式

#### A. 手动添加（适合 1-2 种）

每个 tab 底部的「➕ 添加条目」expander：

- 名称
- 「🛒 标记为「常备免记量食材」」勾选 → 变 boolean 类型（适合葱姜蒜这类永远备着的）
- 否则 → quantity 类型，每份多少克可选
- 易坏 🔴 / 冷冻 复选框
- 初始份数（quantity 时）

#### B. 购物模式（适合一次采购回来）

顶部「🛒 购物模式」toggle 打开后，每行变成「购入份数 → 录入」按钮。逐项填份数 + 点录入，自动累加到现有库存。

#### C. ⚡ AI 批量录入（最快）

顶部 expander 粘贴自由文本：

```
黄瓜3份 羊肉两份 秋葵2份 半份生菜 一包冻虾仁
```

Gemini 解析每项：name + portions + category + item_type + is_perishable。预览面板：
- 🟢 **累加**：库存已有 → 增加份数
- 🔵 **新建**：库存没有 → 创建新条目（AI 推断分类 + 易坏标记，你可手动覆盖）

点「✅ 确认入库」批量执行。

### 状态分组（normal mode）

叶菜/蛋白 tab 在正常模式下会**自动按状态分组**展示：

```
🔴 易坏优先消耗 · 2 项
  🔴 豆苗      ●○○○○○○○○○   1 份  ≈ 500g
  🔴 韭菜      ●●○○○○○○○○   2 份  ≈ 1000g

🟠 囤货较多（≥4 份）· 1 项
  胡萝卜       ●●●●●●○○○○   6 份  ≈ 3000g

🟢 正常库存 · 3 项
  ...

🔘 已用完 · 2 项
  芹菜 / 菠菜
```

`●●●○○○○○○○` 这条**份数填充条**封顶 10 dots，超过加 `+` 后缀。一眼看库存丰俭。

> 💡 购物模式不分组，保留扁平列表方便按原顺序批量录入。

### 常备免记量区（同 tab 内的混合模式）

叶菜/蛋白 tab 内除了主区按份记录的列表，还有一个折叠的「🛒 常备免记量区」放布尔类条目（如葱、香菜、鸡蛋这类永远家里都有的）。点亮表示有货，灰色表示缺。

### 编辑/删除

- **改份数**：直接在 number_input 输入框里键入或用内置 ± 步进，自动保存
- **删除条目**：底部「🗑️ 删除条目」expander（避免误删，常用操作不需要看到删除按钮）
- **改易坏标记**：暂时只能删了重建 *（待优化）*
- **改 portion_weight_g**：暂时只能通过 DB 直接改 *（待优化）*

### 顶部 metric 解读

```
🥬 叶菜/时令         🥩 蛋白冷库          🧂 常备/干货         🔴 易坏优先
   12 份                8 份                15 / 28 有货         3 种
   约 6 天份            约 4 天份                                需优先
```

- **天份**估算按 1 kg 叶菜/天、0.6 kg 蛋白/天
- **常备/干货**显示有货比例（不含主区按份记录的）
- **易坏优先**红字提醒该消耗了

---

## 🌌 食愿之书功能详解

记下命中注定要做的菜。三个集成点让书中之愿**主动降临**：

- 🎲 **推荐器加权**：书中之菜在推荐时 +5.0 权重，自然涌现
- ⭐ **picker 标记**：📅 今日规划手动选菜时书中之菜带 ⭐
- 🧹 **自动消去**：✅ 确认扣减后做过的菜从书中自动移除

**录入流程**：multiselect 选菜（可开「🥕 仅显示库存可做」过滤）→ 可选备注 → 可选目标日期。

**状态徽章**：每条卷宗实时按当前库存计算：
- 🟢 可做 — 主料齐全
- 🔴 缺 X 项 — 显示缺什么

**顶部汇总**包括「缺料汇总」code 块，可复制到 📋 购物清单。

**日期智能**：未来日期的菜**不**进推荐器加权（写"周末做"今天不会跳出来烦你），但仍在 picker 里有 ⭐。

---

## 📋 购物清单功能详解

按超市分别记录、跨店对比、AI 一键解析微信文本。设计原则：贴近你"看广告买"的习惯，**不和菜谱库联动**（你明确说不跟菜谱买）。

### 三个区域

**① 多店并排录入**
```
[+ 新店名]              [➕ 添加]  [🗑️ 清空全部]

🛒 Hmart       🛒 Costco       🛒 Whole Foods
─────────      ─────────       ──────────────
西瓜            冷冻虾, $15      三文鱼
黄瓜            大米
冷饮, $8        冷饮
苹果  红富士
```

- 每行一项；备注用 `,` 或双空格分隔（如 `冷饮, $8` 或 `苹果  红富士`）
- 每个 textarea 自动保存（不需要点保存按钮）
- 数据持久化到 `user_settings.shopping_list` JSON blob，跨重启保留

**② 跨店分析**
```
📊 跨店分析
总需求: 8 项                📍 覆盖最多: Hmart (5/8 项)

🔄 在多家店都列入：
  • 冷饮 → `Hmart` _$8_  ·  `Costco`
```

帮你决定"先去哪家"：
- 优先去覆盖最多的（一站搞定）
- 多店重叠的项可以临时决策（比谁便宜 / 谁顺路）

**③ 📦 采购完成 → 入库**
折叠 expander。选店后，逐项展示该店清单 + 每项可调整购买份数：
```
✅ 我从 [Hmart ▼] 采购完毕

🍉 西瓜             份数: [1]
🥒 黄瓜  $0.99/磅   份数: [3]
🥤 冷饮  $8         份数: [0]   ← 没买
🍎 苹果  红富士      份数: [2]

[📦 入库并清空 Hmart 清单]
```

点击「入库」后：
- **已在库存**的项（如黄瓜、苹果）→ 直接累加份数（无 AI 调用）
- **新条目**（如三文鱼）→ 让 Gemini 自动分类（叶菜/蛋白/...）+ 推断易坏标记 + 设默认 portion_weight_g
- 完成后该店清单清空

> 💡 把"0 份"留给"清单里写了但当天没买/缺货"的项，下次回来还在清单里。

### ⚡ AI 解析 WeChat 文本（强烈推荐）

折叠 expander 里。粘贴你 WeChat 风格的自由文本：

```
hmart: 西瓜, 黄瓜, 冷饮 $8, 苹果
costco: 大米, 冷冻虾 $15
whole foods: 三文鱼
```

`gemini-flash-lite-latest`（高配额，不联网）解析为结构化数据，预览后**累加**到现有清单（不覆盖）。完美适配"WeChat 写完整段粘进来"的工作流。

> 💡 累加而非覆盖：你周一在 Hmart 列里写了 3 项，周三 AI 解析又加 5 项，会变成 8 项，不会丢前面 3 项。

### 典型工作流

**周日**：刷超市广告 → WeChat 边看边写 → 整段粘进 AI 解析 → 一次入库

**周二路过 Costco**：开 app → 看 Costco 列表 → 顺手买 → 回来清空那栏

**周末**：看「📊 跨店分析」决定主目标店 → 出发

---

## 🍳 菜谱库功能详解

### 列表筛选

顶部 4 个 selectbox + 搜索框：

| 筛选 | 用法 |
|---|---|
| 🔍 搜索 | 菜名关键字，子串匹配 |
| 🥩 荤素 | 纯蛋白 / 半蛋白半素 / 纯素 |
| 🍳 烹饪方式 | 炒/蒸/烤/煮/汤/凉拌/炸/炖/煎 |
| 🍱 种类 | 菜肴/主食/甜点/早餐/饮料/冷冻/预制 |

筛选可叠加，结果按 `created_at DESC` 排序（最新加的在前）。

### 顶部三按钮

- **➕ 新增**：手动填表单（适合手头有现成菜谱想结构化录入）
- **⚡ AI 入库**：粘贴自由文本，Gemini 解析（适合从微博/小红书截下来的菜谱）
- **🗄️ 食材营养库**：直跳到营养分析 → 食材库 tab，检查/编辑营养数据

### ⚡ AI 入库流程

三步走：

**Step 1：粘贴原文**

```
180g肉末，250g豆腐干切丁，120g毛豆。葱姜蒜小米辣香菜。
调料mix：料酒10g，生抽10g，老抽10g，味精1g，糖5g。
不粘锅煸炒肉末出油，加葱姜蒜黄豆酱30g，小火翻炒，
加调料mix+豆腐干+200ml水，炖煮5分钟，加毛豆，水淀粉收汁。
```

格式不限，自由发挥。Gemini 会处理单位换算（tsp/tbsp/lb/cup）、葱姜蒜默认克数（10/5/10g）、步骤重写（含克重内嵌）。

**Step 2：预览校对**

显示：菜名 / 实操+等待时间 / 菜系 / 分类 / 烹饪方式 / 调料摄入 / 中英文简介 / 主料调料拆分 / 步骤。

肉眼扫一遍。明显错误就「← 重新输入」改原文再解析；细节问题（如 cuisine 想从家常改日式）入库后再编辑。

**Step 3：营养查询**

「✅ 确认入库」后会出现「🔍 触发营养查询」按钮。它会对所有主料调用四级降级查询，缺失的食材显示 CLI 提示，方便事后用 `seed_nutrition_ai.py --ingredient XXX` 补。

> 💡 **AI 解析的局限**：步骤格式偶尔会回退到"动作 + 调料括号"的旧格式不完全；菜系判断有时偏保守（很多菜会归"家常"）。这些都是入库后 30 秒就能手动校准的小事。

### 编辑表单关键字段

| 字段 | 怎么填 |
|---|---|
| **菜名** | 简洁 4-8 字，避免长定语 |
| **备菜难度** | 简单 / 中等 / 繁琐 |
| **实操时间** | 你**必须站在灶边**的时间（切菜+翻炒+调味） |
| **等待时间** | 不用看守的时间（腌制/炖煮/蒸/发酵）；纯快炒填 0 |
| **烹饪方式** | 可多选，"汤"/"凉拌"放这里**不是** category |
| **占用炒锅** | 是否需要中式炒锅。推荐器用此做约束（一餐 ≤1 标准+1 轻占） |
| **可并行** | 烤/蒸/炖等设备型菜可同时进行其他操作 → 推荐器知道时长可叠加 |
| **荤素** | 留空可，影响推荐器肉类多样性策略 |
| **形态** | **必填**，菜肴/主食才会出现在晚餐推荐池 |
| **菜系** | 决定菜系联动（详见进阶技巧） |
| **强搭配** | multi-select 其他菜谱，详见进阶技巧 |
| **英文菜名 / 中文简介 / 英文简介** | PDF 餐厅菜单效果用 |
| **调料摄入比例** | 调料实际被吃下的比例，详见进阶技巧 |
| **个人摄入份量** | 每人吃多少，详见进阶技巧 |
| **数据质量** | 完整 / 待补全 / 估算，影响列表标记 |

### 详情页

显示菜谱所有信息 + 顶部操作：✏️ 编辑 / 🔄 标记今天做过 / 🗑️ 删除（需确认）。

**🔄 标记今天做过**：写入 `last_cooked` 时间戳，推荐器会避开 48h 内做过的菜（防厌倦）。

**🍱 强搭配**显示为可点击卡片，点击直接跳转到搭配菜的详情页。

### 🔍 语义搜索

折叠 expander，基于 ChromaDB + sentence-transformers (`paraphrase-multilingual-MiniLM-L12-v2`)。

**何时用**：
- 关键字搜不到（"清淡蔬菜汤" vs 实际菜名"上汤娃娃菜"）
- 概念检索（"适合冬天的暖身菜"、"高蛋白低脂的减脂菜"）

**何时不用**：知道具体菜名就用顶部搜索框，更快更准。

> 💡 首次使用需点「初始化索引」按钮（一次性建 embedding），或 CLI `python3.9 scripts/build_recipe_embeddings.py`。新加的菜会自动增量索引。

---

## 📅 今日规划功能详解

页面由 4 个 section 构成（从上往下）：**推荐 → 今日菜单 → 确认扣减库存 → 选菜面板**。

### 🎲 智能推荐器

点「🎲 推荐组合」生成 1-2 个候选套餐。推荐器目标：**1 凉拌 + 2 热菜 + 1 汤**，按以下顺序填 slot：

1. 汤池：1 个含「汤」cooking_method 的菜
2. 凉拌池：1 个含「凉拌」cooking_method 的菜
3. 热菜 slot：按荤素覆盖（纯蛋白优先）填 2 个
4. 不够的位置：从全池补足

**硬过滤**（不满足直接淘汰）：
- 主料**全部**在库存中（quantity > 0 或 in_stock = true）
- 48h 内未做过（last_cooked 时间戳）
- 仅形态为「菜肴」或「主食」的菜参与（甜点/饮料/早餐排除）
- 单菜总时长（`active + idle`）不超过「⏰ 单菜上限」预算（默认 90min）

**软偏置**（影响权重）：
- 每份主料 +3.0（覆盖易坏食材）+1.0（消耗囤货 ≥4 份）
- `is_parallel=true` +1.5
- 烹饪方式 ≥2 种 ×0.4（鼓励简单菜）
- **菜系联动**：同已选菜的 cuisine ×2.5
- **强搭配**：已选菜的 pairing_ids +5.0

**硬约束**（违反则整套被拒）：
- 炒锅冲突：≤1 道标准占锅（> 5min 实操）+ ≤1 道轻占锅（≤ 5min 实操）
- ≤1 道繁琐难度

**3-pass 系统**：第一轮强制覆盖所有易坏食材；第二轮放松易坏；第三轮允许套餐重叠。

### 推荐卡片解读

```
1. 凉拌黄瓜
   :gray[凉拌]
2. 红烧带鱼 🍳
   :red[纯蛋白] · :blue[菜肴]
3. 蒜蓉西兰花
   :green[纯素] · :blue[菜肴]
4. 紫菜蛋花汤
   :blue[菜肴]
────────────────────
⏱️ 约 65min  难度：中等
🍲 含汤
🍱 菜系联动：家常       ← ≥2 道同菜系才出现
🔴 覆盖易坏：豆苗、韭菜
[✅ 采用此套餐]
```

- 🍳 = 占用炒锅 / ⚡ = 可并行
- 时长：有并行菜显示「并行约 Xmin」，否则「⏱️ 约 Xmin」
- 警告：未覆盖的易坏食材会显示 ⚠️

「✅ 采用」会把 4 道菜导入「📋 今日菜单」，覆盖现有选择。

### 📋 今日菜单 + 备餐控制台

已选菜显示为列表，每行末尾 ✕ 可删除。系统会自动校验：

- **🍳 炒锅冲突**：选了 2 道标准占锅菜会红字警告
- **🔄 防厌倦**：48h 内做过的菜会黄字提示
- **🔴 易坏未覆盖**：库存里有易坏菜但今天不消耗会建议加菜

**🎛️ 备餐控制台**（折叠 expander）：
- 🍚 主食：白米饭 / 不吃 / 自定义
- 🍎 水果：multi-select + 每种克数
- ➕ 临时加菜：自由文本（如「莴笋 150g\n金针菇 100g」），会和菜肴一起 ÷2 算每人份

> 💡 控制台的 widget **跨页绑定**——你在这改完，切到营养分析页设置也变了，反之亦然。打印 PDF 时即时（JIT）计算最新营养，无需先去营养页点"计算"。

### 🖨️ 打印 PDF

按钮点击 → 自动调用最新备餐控制台状态算营养 → 生成两面 PDF → 自动打开。

打印顺序：**凉拌优先 → 热菜 → 汤菜最后**（符合上菜顺序）。

### ✅ 确认扣减库存

「📋 今日菜单」下方「✅ 确认完成今日菜单」按钮（用于晚餐结束后）：
- 自动扣减所有非调料主料对应的库存份数
- quantity 项：减相应份数
- boolean 项：保持不变（常备品不消耗）
- 标记菜谱 `last_cooked` = now（触发 48h 防厌倦 + 24h 易坏例外）

### 🛒 选菜面板（Picker）

折叠区，手动加菜用。顶部从上到下：

1. **💡 推荐搭配 banner**（条件出现）：当已选菜有 `pairing_ids` 时，未选的搭配菜一键加入
2. **🍱 已锁定菜系 hint**（条件出现）：当已选菜有 cuisine 时，提示当前菜系
3. **🥕 仅显示可做的菜** toggle：开启后只显示库存够做的菜（严格匹配，未录入食材 = 缺货）
4. **5 个 selectbox** 过滤：搜索 + 荤素 + 烹饪方式 + 种类 + 菜系
5. **菜谱列表**：每行末尾「＋ 加入」按钮 / 已选则显示「✓ 已选」

### 临时占位菜（Placeholder）

如果今天想吃菜谱库里没有的菜，又懒得新建菜谱：使用「✏️ 临时占位菜」按行输入 `食材名 数量 单位`，参与营养计算和库存扣减，但**不进菜谱库**，吃完即弃。

---

## 📊 营养分析功能详解

页面有 **5 个 tab**：

```
🌅 全日营养  |  🍽️ 菜谱营养计算  |  🔍 食材查询  |  🗄️ 食材库  |  📈 历史记录
```

### 🌅 全日营养（最常用）

按"早 → 午 → 水果 → 晚 → 主食"构建一天的总摄入，对比 14 种营养素的 DRI。

**早餐 / 午餐**：
- 默认值：硬编码的家常组合（早 580kcal 杂粮豆+燕麦+蛋奶，午 250kcal 高蛋白可可饮+坚果），从 `_BFST_BASE`/`_LUNCH_BASE` 来
- 不在家吃：「今日不在家吃X饭」toggle → 该餐归零
- 吃了别的：「✏️ 特殊情况：自定义食材」expander 按行输入，**覆盖默认值**

**水果**：multiselect + 每种克数（默认 65g/种，建议 2-3 种）

**晚餐菜肴**：从「📅 今日规划」的 `plan_rids` 导入，显示每道菜的 `serving_ratio %` 提示

**晚餐主食**：白米饭（默认 100g 熟）/ 不吃 / 自定义

**晚餐临时加菜**：自由文本，应对"乱炖里加莴笋"等场景

点蓝色「📊 计算今日全日营养」→ 显示：
- 4 个 metric：热量 / 蛋白质 / 脂肪 / 碳水（每人）
- 14 条 DRI 进度条（钠反向：> 1500mg ⚠️ / > 2300mg 🚨）
- 各餐明细表 + 食材快照

**💾 保存今日记录**（蓝色按钮）→ 写入 `daily_logs` 表，供历史记录页分析。

**🤖 AI 营养建议**（折叠 expander）：Gemini 给针对性建议，3 次/天限额（按日重置）。

### 🍽️ 菜谱营养计算

独立计算单道菜或几道菜组合的营养，三种输入：

- **📚 从菜谱库选择**：multi-select 菜谱
- **📅 从今日规划**：用当前 `plan_rids`
- **✏️ 临时输入食材 (Placeholder)**：自由文本

点「🔍 开始计算」→ 显示完整 Macros + Micros + 食材明细表（含 USDA fdcId 🔗 链接）。

### 🔍 食材查询

输入食材名 + 克数，单条查询营养。用于：
- 验证 USDA 匹配是否正确（点 🔗 跳转 USDA FoodData Central 核对）
- 临时算单个食材的营养

四级降级实际生效顺序在结果上方有标注（如 `匹配来源：local_nutrition.json`）。

### 🗄️ 食材库

`nutrition_cache` 表的 CRUD UI：
- 查所有缓存条目
- 编辑（修正 USDA 错配，如冬瓜被匹配成冬瓜糖）
- 删除（让下次查询重新走四级降级）
- **🔄 同步 local_nutrition.json**：把 seed 脚本写的 JSON 条目导入 SQLite 缓存，使其可在 UI 编辑

> 💡 营养数据有误怎么办：菜谱库点「🗄️ 食材营养库」直跳过来 → 找到那个食材 → 直接改 → 保存。

### 📈 历史记录

显示最近 14 天的：
- 折线图：热量、蛋白质、钠、纤维趋势
- 7 日食材多样性：唯一食材数 / 唯一水果数 / 日均
- **7 日 DRI 热力图**：14 种营养素 × 7 天表格，每格 ✅🟡🔴 + 百分比

`✅ ≥80% DRI · 🟡 50–79% · 🔴 <50%`（钠反向：✅ <65% · 🟡 65–99% · 🔴 ≥100%）

旧记录没有 `total_nutrients_json` 字段 → 回退到只显示 6 个宏量营养素。

---

## 🎯 进阶技巧

### 菜系联动怎么用最好

每个菜谱编辑时都给它打 `cuisine` 标签（日式/韩式/川菜/家常/...）。推荐器会**自然涌现**同菜系套餐——不需要你手动指定，第一道菜被随机选中后，后续 slot 会自动 ×2.5 偏向同菜系候选。

**让联动发挥效果的前提**：
- 每个菜系**至少 3-4 道菜**，否则池子太小凑不齐套餐
- **凉拌/汤/热菜各菜系都要有覆盖**，否则结构化 slot 填不满会回落到"随便选"
- 不想形成套餐的菜不打菜系标签（如随便炒的家常菜，留空即可）

### 强搭配（Recipe Anchoring）设置思路

打开菜谱编辑页 → 「🍱 菜系 & 强搭配」 → 「强搭配菜谱」multi-select。

**典型搭配**：
- 鳗鱼饭 → 黄瓜小菜、味噌汤
- 火锅 → 蘸料、芝麻烧饼
- 川菜麻辣类 → 凉拌黄瓜（解辣）
- 烤鱼 → 拍黄瓜、毛血旺

**生效场景**：
- 自动推荐时：被选中的菜的 pairing 在后续 slot 中权重 **+5.0**（比菜系联动更强信号）
- 手动选菜时：选中后顶部出现「💡 推荐搭配」banner，一键加入

### condiment_ratio 怎么估

每道菜独立的"调料实际被吃进去的比例"：

| 场景 | 比例 |
|---|---|
| 炒蔬菜 / 盐腌 / 清蒸 | 100% |
| 蒸鱼 / 腌料 | 50% |
| 红烧 / 炖肉（汤汁不喝）| 15-25% |
| 涮锅 / 汤底 | 5-15% |

这个值直接影响晚餐钠摄入计算。新菜默认 100%，按实际酌情调低。

### serving_ratio 怎么定

"相对于菜谱总量 ÷ 2 的个人摄入比"。默认新菜 50%。

| 场景 | 比例 |
|---|---|
| 大份汤锅只喝一小碗 | 25% |
| 共享菜小份取 | 50%（默认） |
| 正常一人份炒菜 | 50%-100% |
| 你一个人能干掉半锅 | 100% |

### 数据质量维护节奏

DB 里约 149 条菜谱仍是 `needs_review`（调料未结构化）。建议节奏：

- **新菜入库**：用「⚡ AI 入库」自动生成结构 → 试做一次 → 好吃就编辑校准、不好吃就删
- **存量待补**：每周空闲花 10 分钟编辑 1-2 道老菜（添 cuisine / pairing / 拆 active+idle time）
- **营养缺口**：菜谱库列表点「🗄️ 食材营养库」→ 看缺数据的食材 → CLI `python3.9 scripts/seed_nutrition_ai.py --ingredient XXX` 补

---

## ❓ FAQ

### Q：推荐总是出现同样几道菜怎么办？

**可能原因 + 解法**：
1. **菜谱库覆盖度不够**：可做的菜池子太小。看 📦 库存 → 哪些主料常缺货 → 补库存或新增对应菜谱
2. **防厌倦没生效**：检查这些菜的「上次烹饪」时间。如果你做完忘了点「✅ 确认完成今日菜单」，`last_cooked` 不会更新，48h 防厌倦就不触发
3. **权重失衡**：某些菜易坏食材覆盖太多 → 权重过高。手动调整菜谱的食材结构或干脆删掉

### Q：AI 解析菜谱步骤格式错了

**应对**：
- **结构性错误**（食材克数错、分类错）：「← 重新输入」修改原文中的歧义部分（如"少许"→"5g"）重新解析
- **格式细节**（步骤没内嵌克重、菜系判断保守）：直接确认入库后在编辑页修。Gemini 偶尔会回退到老格式，30 秒手动校准就好

### Q：USDA 查不到的食材怎么办

**首选 → UI 内 AI 录入**：📊 营养分析 → 🗄️ 食材库 → 顶部「⚡ AI 录入营养数据」expander。两种模式：

- **A) 偷懒模式** — 粘食材名（如「黄豆酱、冬瓜、芥兰苗」），Gemini 联网查 USDA / 中国食物成分表（耗 quota）
- **B) 精确模式** — 你已经在网上找到了权威数据，直接把那段原文（带数值的成分表）粘进来，Gemini 解析数值不联网（**省 quota、更准**，推荐用于已知 USDA 错配的修正）

预览面板可以手动编辑数值后入库。

**其他途径**：
1. **CLI 批量补**：`python3.9 scripts/seed_nutrition_ai.py --ingredient 冬瓜糖`（适合一次补几十个，会写入 `local_nutrition.json`）
2. **手动加 `data/local_nutrition.json`**：四级降级的第二级，最直接但要懂 JSON 格式
3. **UI 食材库表格直接改**：已有缓存 → 直接在 data_editor 改数值 → 保存（最快修小错）

### Q：seed 脚本写入的营养数据有错怎么办

新流程：用 UI 内的「⚡ AI 录入营养数据」（食材库 tab 顶部）重新查询该食材 → 状态会显示 🟢（覆盖）→ Gemini 联网重新跑 + 你手动校正 → 入库覆盖错值。

如果是 seed 脚本写错的特定字段（如 USDA 匹配是对的但某个维生素值离谱），直接在食材库表格里改那一格更快。

### Q：营养数据明显不对怎么核验

任意菜谱营养计算结果的食材明细表里，每行末尾的 🔗 链接直接跳到 USDA FoodData Central 原始页面。对照原始数据看是否匹配错了。匹配错了直接在「🗄️ 食材库」改 `name_en` 字段或干脆改数值。

### Q：怎么备份数据

**最简单**：复制 `data/diet.db` 文件。SQLite 是单文件，整个 app 状态（菜谱、库存、营养缓存、每日记录、设置）都在里面。

**完整备份**（含语义搜索索引）：复制整个 `data/` 目录：
```
data/
├── diet.db                       ← 主数据库
├── local_nutrition.json          ← 营养数据覆盖
├── ingredient_translations.json  ← 中英翻译
└── chroma/                       ← ChromaDB 索引
```

### Q：换电脑怎么迁移

1. 新机器装 Python 3.9 + 依赖：`pip3.9 install -r requirements.txt`
2. `.env` 文件复制过去（含 GEMINI_API_KEY / USDA_API_KEY）
3. `data/` 目录整个拷贝过去
4. `python3.9 -m streamlit run app.py` 启动

如果迁移后 schema 旧了：`python3.9 db/init_db.py`（幂等，自动执行 migration step 1-14）

### Q：Gemini 配额用完了怎么办

不同功能用不同模型，配额独立：
- **AI 入库 / 批量录入**：`gemini-flash-lite-latest`（高配额，基本不会到顶）
- **营养顾问 (3次/天)**：`gemini-2.5-flash`，应用层限制保护，按日重置
- **seed_nutrition_ai.py**：`gemini-2.0-flash`（1500 RPD），跑完每天的限额可以等隔天

如果某模型 429 了：临时跳过，几小时后再试；或在代码里换成 `gemini-flash-lite-latest`。

### Q：语义搜索初始化失败 / 不出结果

1. 首次使用需建索引：`python3.9 scripts/build_recipe_embeddings.py`，或菜谱库「🔍 语义搜索」expander 里点「初始化索引」按钮
2. 模型首次加载需下载 ~120MB（`paraphrase-multilingual-MiniLM-L12-v2`），断网会失败
3. 索引持久化在 `data/chroma/`。删除该目录重建即可重置

### Q：推荐器一直返回空套餐

**Debug 顺序**：
1. 菜谱库里至少要有 4 道（晚餐推荐目标 4 道菜）形态为「菜肴」或「主食」的菜
2. 这些菜的主料**全部在库存**（quantity > 0 或 in_stock = true）
3. 没有全部 48h 内做过

库存空的话先去 📦 录入。菜谱主料如果包含库存里没录的食材（如"蔓越莓干"这种），推荐器会跳过该菜——可以把这种食材作为「常备免记量」加入库存绕过限制。

### Q：份数和克数之间怎么转换

每个 quantity 类型的库存条目有 `portion_weight_g`：
- 添加时手动设（默认叶菜 500g、蛋白 300g）
- 显示时自动换算 `X 份 ≈ Yg`

目前修改已有条目的 `portion_weight_g` 需要直接改 DB（待优化为 UI 操作）。临时方案：删了重建。

---

*本手册随 app 演进同步更新。最后更新：见 git log。*
