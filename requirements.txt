# Python >= 3.9 required (system `python` may be 2.7 on macOS — use python3.9)

# ── Core ─────────────────────────────────────────────────
streamlit>=1.32.0
python-dotenv>=1.0.0

# ── Nutrition API (Phase 4) ───────────────────────────────
requests>=2.31.0

# ── AI / ML (Phase 6) ────────────────────────────────────
# google-generativeai>=0.7.0  (using google-genai SDK instead)
sentence-transformers>=2.7.0
chromadb>=0.5.0

# ── PDF generation (Phase 8) ─────────────────────────────
reportlab>=3.6.0

# ── Scraping (Phase 3 — uncomment before running scraper) ────
# beautifulsoup4>=4.12.0
# lxml>=5.0.0
